Public Class MacroScanner

    Dim tokens As LinkedList(Of String)

    Public Sub scan(buffer As String)

        'Loop through input string
        For Each c As Char In buffer

        Next

    End Sub
End Class
